#include "normalization.h"

void Normalization::processeBuffer(unsigned char* buffer, size_t bufferSize, int scale) 
{
     unsigned char* outputBuffer;

     for(int i = 0; i < bufferSize; i++){
     auto maxValue = std::max_element(buffer)


     }
}

